-- Create a table named car
CREATE TABLE car (
  id INT PRIMARY KEY, -- the id of the car
  brand VARCHAR(50) NOT NULL, -- the brand name of the car
  yer VARCHAR(4) NOT NULL, -- the year of the car
  price VARCHAR(10) NOT NULL -- the price of the car
);
